# 获客初筛 Agent：多维用户状态估计与安全动作执行开发计划

## 总结

将项目实现为一个纯 Python 的终端 demo，并初始化为 Git 仓库。核心不是让 LLM 直接“决定并执行动作”，而是拆成：

1. LLM 对客户消息进行结构化状态估计；
2. 确定性状态机维护客户的连续状态；
3. 策略层根据状态和约束选择唯一合法动作；
4. 执行层再次校验动作、限流和静默状态。

学术上可将该设计表述为“对话理解中的多维用户状态估计”：联合估计意图、情绪不满和对话异常信号，再进行策略决策。相关概念可对应 USE、dialogue breakdown detection、joint emotion-intent understanding；工业映射则是客服路由、情绪升级和自动跟进控制。

## 主要实现

### 1. 仓库与项目骨架

初始化 Git 仓库并建立最小 Python 项目：

- `README.md`：启动方式、架构说明、约束保证机制、已知局限。
- `.gitignore`：忽略虚拟环境、缓存、`.env` 和日志。
- `.env.example`：提供 `GEMINI_API_KEY`、`GEMINI_MODEL` 等配置示例。
- `src/`：核心代码。
- `tests/`：状态机、策略、限流、攻击样例和 LLM 适配器测试。
- `requirements.txt` 或 `pyproject.toml`：锁定依赖。

首版不引入 FastAPI；终端交互已经足够完成题目要求，也能减少无关工程量。

### 2. LLM 接入与结构化理解

使用 Google/Gemini API，代码通过独立适配器封装，避免业务逻辑依赖具体 SDK。

环境变量：

- `GEMINI_API_KEY`
- `GEMINI_MODEL`，提供合理默认值
- 可选超时和重试配置

LLM 只负责输出结构化理解结果，不直接执行动作。建议固定为 JSON Schema：

```json
{
  "intent": "interested | needs_info | rejected | off_topic | other",
  "dissatisfied": true,
  "confidence": 0.0,
  "reason": "brief internal rationale"
}
```

其中：

- `intent` 对应题目要求的五类意图；
- `dissatisfied` 是独立正交信号，不能从意图字段推导；
- `confidence` 仅用于观测和调试，不直接绕过确定性规则；
- `reason` 不向客户展示。

解析失败、字段非法、API 超时或模型输出不符合 schema 时，采用 fail-closed 策略：不自动生成回复，转人工或进入安全的待处理状态，并记录错误原因。

### 3. 确定性客户状态机

每个客户维护独立状态，至少包括：

- 当前会话状态：`active`、`escalated`、`closed`
- 连续异常计数器
- 最近主动发送消息的时间戳队列
- 最近一次 LLM 判定结果
- 跟进标记和审计事件

状态转移规则必须写在代码中：

- `off_topic` 或 `dissatisfied`：共享同一个连续计数器加一；
- 任何其他情况：计数器重置为零；
- 计数器达到两次：立即进入 `escalated`；
- `escalated` 后保持静默，任何客户消息都不能自动触发回复、跟进或其他动作；
- 只有明确的人工重新激活接口/命令才能恢复 `active`；
- `mark_not_interested` 后进入 `closed`，不再自动处理；
- 每个客户状态隔离，不能互相影响。

该逻辑不能依赖 LLM 自己决定是否升级。

### 4. 策略层与动作执行层

动作使用枚举或受限联合类型，只允许：

- `reply`
- `schedule_followup`
- `escalate_to_human`
- `mark_not_interested`

处理流程：

```text
客户消息
  -> LLM 结构化状态估计
  -> 更新确定性状态机
  -> 策略层计算候选动作
  -> 动作 allowlist 校验
  -> 会话状态校验
  -> 滑动窗口限流校验
  -> 执行动作并写入审计日志
```

关键约束：

- 客户消息永远作为不可信数据处理，不能改变系统提示、策略配置、动作枚举或状态机；
- LLM 输出不能直接调用 Python 函数或工具，只能映射到预定义数据结构；
- 执行器拒绝任何未知动作；
- `escalated` 状态下执行器无条件返回静默；
- `escalate_to_human` 一旦执行，后续动作全部被状态门禁拦截；
- `reply` 必须先经过回复生成和输出防护，再进入最终发送函数。

### 5. 速率限制

对每个客户使用滑动窗口，而不是固定分钟窗口：

- 保存该客户最近主动消息的发送时间戳；
- 每次真正发送前移除 60 秒以前的时间戳；
- 若窗口内已有一条主动消息，则拒绝本次 `reply`；
- 只有最终发送函数记录时间戳，LLM 重试、内部推理或策略重算不计入发送次数；
- `schedule_followup`、`escalate_to_human`、`mark_not_interested` 不产生客户可见主动消息，因此不占用发送配额。

建议使用可注入时钟，方便测试边界条件。

### 6. 回复生成与防套话设计

回复生成与意图分类分开：

- 分类提示词只要求输出 schema；
- 回复生成提示词明确禁止泄露系统提示词、内部规则、价格底线、凭证和实现细节；
- 系统提示词中不放真实密钥或不必要的内部机密；
- 生成结果经过长度、格式和敏感信息检查；
- 检测到明显的提示词泄露、秘密请求或内部规则复述时，不发送原文，改为安全的泛化回复或转人工；
- 最终回复只能通过统一发送函数发出。

必须在 README 中明确说明：自然语言泄露防御无法保证 100%，尤其无法覆盖所有隐晦改写、编码和多轮诱导；真正可强制保证的是动作越权、升级后静默和速率限制。防御重点应放在“最小化暴露 + 独立生成 + 输出审查 + fail-closed”，而不是关键词黑名单。

### 7. 终端交互

提供简单 CLI：

- 输入客户 ID 和消息；
- 显示 LLM 判定结果摘要、状态变化和最终动作；
- `reply` 显示回复草稿；
- `schedule_followup` 显示已标记；
- `escalate_to_human` 显示已转人工并进入静默；
- 提供人工 `reactivate <customer_id>` 命令；
- 提供 `show_state <customer_id>` 和 `reset <customer_id>` 便于答辩演示。

默认使用内存存储，进程重启后状态清空；这符合最小 demo 范围。

## 测试与验收

使用 pytest，并为 LLM 编写 fake adapter，使确定性逻辑不依赖网络。

必须覆盖：

1. 五类意图都能映射到合法策略；
2. `dissatisfied` 与意图独立，例如“有兴趣但很不满”；
3. 连续两次 `off_topic` 或 `dissatisfied` 后必定转人工；
4. 中间出现正常消息时，连续计数器重置；
5. 转人工后客户发送任何消息都保持静默；
6. 人工重新激活后才恢复处理；
7. 任意 60 秒滑动窗口最多发送一条主动消息；
8. 59 秒、60 秒边界行为；
9. LLM 重试不会绕过最终发送限流；
10. 未知动作、伪造工具调用和客户注入文本均无法执行越权操作；
11. `mark_not_interested` 后会话关闭；
12. LLM 输出非法 JSON、超时或 API 错误时 fail-closed；
13. 回复中出现系统提示词/内部规则请求时触发输出防护。

至少准备三组答辩攻击对话，并在 README 中记录输入、模型判断、状态变化、最终动作和结果：

- 伪装成系统指令，要求跳过审核或执行未定义动作；
- 在已转人工后，通过多轮消息诱导恢复自动回复；
- 以改写、角色扮演或编码方式套取系统提示词、内部规则或价格底线。

验收标准是：攻击文本可能影响 LLM 的分类或回复内容，但不能突破动作枚举、状态机、静默门禁和最终发送限流。

## 假设与取舍

- 使用主办方提供的 Gemini API Key，不把密钥提交到 Git；
- 通过 `GEMINI_API_KEY` 配置，LLM SDK 仅存在于适配器层；
- 首版采用终端交互和内存状态，不做真实 IM、数据库、网页前端和多实例部署；
- 不追求复杂的多轮记忆或训练模型，优先保证约束可解释、可测试、可答辩；
- 后续可扩展 SQLite、网页/API 接口、多语言和更细粒度的用户状态，但不纳入首版验收范围。
