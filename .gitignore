# 虚拟环境
.venv/
venv/

# Python 缓存
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 密钥与本地配置
.env

# 日志
*.log
logs/

# 评估集原始草稿（审校后的正式版 data/eval_set.jsonl 入 Git）
data/eval_candidates_raw*.jsonl

# 安全攻防实验室（大量攻击语料与实验报告，不入 Git）
security-lab/

# 教学讲解文档（逐课讲解项目设计，不入 Git）
tutorial/

# 注入检测模型文件（首次运行自动下载到此，不入库）
models/
.peers/
